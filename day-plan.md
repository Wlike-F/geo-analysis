# 地质测井数据可视化系统 - 开发日计划 (Day Plan)

为了高效、高质地完成系统开发任务，特将项目开发进程拆解为为期 6 天的研发周期。

## Day 1: 需求对齐、项目脚手架搭建及数据库设计
- **任务:**
  1. 输出最终版总架构设计及UI蓝图。
  2. **后端搭建:** 初始化 Spring Boot 工程，集成 Nutz、MySQL、Redis。
  3. **前端搭建:** 初始化 Vue 3 + Vite 工程，集成 UI 组件库（Element Plus），并配置基本路由（login/home）。
  4. **数据库设计:** 建立 `user` 等核心系统表。

## Day 2: 基础 UI 框架与用户模块 (参考原图设计)
- **任务:**
  1. **前端:** 完成**登录注册页面**的设计，要求整洁大气。
  2. **前端:** 搭建后台管理系统的**主布局页面 (Layout)**（涵盖左侧菜单，顶部 Header 和右侧主路由视图 `router-view`）。
  3. **后端:** 开发并调试 JWT 登录/鉴权接口、注册接口。
  4. **前后端联调:** 完成系统身份认证环节。

## Day 3: UI 深入与“文件加载双入口”模块
- **任务:**
  1. **前端:** 开发主界面的“卡片式操作区”（参考设计图），添加两类操作按钮：上传本地TXT、读取服务器指定路径TXT。
  2. **前端:** 集成 **Tab标签页组件**，用以承载未来加载的多个不同文件。
  3. **后端:** 实现处理多文件上传接的 API。
  4. **后端:** 实现基于入参/配置，扫描指定本地路径并返回 JSON 文件名列表的接口。

## Day 4: 核心功能之“数据可视化与精度处理”
- **任务:**
  1. **后端:** 实现读取单一TXT文件内容并将其解析为 List 对象的工具类；严格处理四列属性：`DEPTH`, `AC`, `DEN`, `GR`。
  2. **前端:** 构建文件对应的面板内容，引入具有4列的数据表格（Table）。
  3. **精度联调:** 前后端同步核准数据精确到小数点后四位 (`.0000`) 的显示要求。

## Day 5: 阈值筛选过滤与 Excel 下载模块
- **任务:**
  1. **前端:** 在表格区域新增各列的“阈值输入框并排布局”，实现条件绑定。
  2. **逻辑处理 (可选):** 如果数据量不大可采用前端过滤；若数据量大采用后端过滤。建议开发后端通过条件对数据实施动态过滤过滤的接口。
  3. **后端:** 集成 EasyExcel/POI，通过接口传入 `过滤条件 + 文件标识`，在内存中生成并返回对应的 `.xlsx` 数据流实现下载。一个 TXT 对应生成一份过滤后的 Excel 文件。

## Day 6: 产品打磨、UI 美化与整体测试
- **任务:**
  1. **UI与交互:** 深度参照原型美化组件（如按钮颜色、卡片阴影、统计小组件和左栏头像展示等整体美观大气的细节）。
  2. **功能测试:** 检查特殊用例下的过滤准确性和文件下载规范。
  3. **交付准备:** 清理无用代码，补充核心类注释，完成部署环境测试，并确认是否达成所有需求目标。

## 新增业务需求规划: 异常地层连续深度区间与物理厚度智能提取
- **需求背景与地质测井意义:**
  在实际地质测井数据分析中，地质专家通常极少去单独审视某个游离点，而是更关心**整块异常地层（如主力储层、特殊岩性）在地下所处的具体位置及其跨度**。例如，当观测到自然伽马（GR）连续大于200时，说明此刻钻头正连续穿过放射性较强的高泥质岩石层。我们必须直观地报告出“这个岩层有多厚？”、“它位于地下多深的空间？”，仅展示过滤后的零散结果已经无法满足专业需求，需将散点数据整合成地质学上的连续“段”模型。

- **核心逻辑与系统开发设计:**
  1. **连续阈值分段识别 (Segment Recognition):**
     * 基于用户在界面的动态阈值输入（如 `GR > 200` 等），系统在后端遍历整个文件的测井序列。
     * 当数据点符合条件并“连续”出现时，在后台将它们视为一个“地层段”（Segment）。遇到不符合条件的值时中断，以此对原本杂乱的文件切分出“第一段”、“第二段”等独立区块。
  2. **深度数据坐标拾取 (Depth Coordinate Extraction):**
     * 从第一列参数 `DEPTH` 抓取锚点。
     * 针对每个被切分的异常地层段：
       - 取该段第一行记录的 `DEPTH` 值为 **起始深度 (Start Depth)**
       - 取该段最后一行记录的 `DEPTH` 值为 **终止深度 (End Depth)**
  3. **物理厚度高精度测算 (Stratum Thickness Calculation):**
     * 计算公式模型为：**该层厚度 = (该层终止深度 - 该层起始深度)**。
     * 以截图举例：段头位于 `1660.125m`，段尾位于 `1662.500m`，得出该放射层物理厚度为 `1662.500 - 1660.125 = 2.375m`。最终保持 4 位精度。
  4. **产品UI交互与分析报表排版 (UI & Data Presentation):**
     * 因为单口井往往会穿透多个类似岩层，系统必须提供一种**报表级**或**侧边栏抽屉级**的归纳呈现形式，清晰罗列出所有挖掘出结果。该报表提供下载功能，可单一下载、可打包zip下载；
     * **展示范本：**
       > **文件 LT2.txt 地质阈值 (GR > 200) 分析报告**
       > - **[第 1 段 地层异常]** 起始深度: `1660.125`m — 终止深度: `1662.500`m，地层厚度估算: **`2.3750`**m
       > - **[第 2 段 地层异常]** 起始深度: `1680.125`m — 终止深度: `1685.000`m，地层厚度估算: **`4.8750`**m
       >   ...