# 海量测井数据批量导出落地方案

针对前端浏览器由于 V8 引擎堆内存限制而无法直接在客户端一次性处理和打包几百兆以上数据的瓶颈，如果必须要实现“**批量导出打包**”功能，我们可以采用以下三种不同技术侧重的架构方案。

---

## 方案一：后端异步流式打包（业界标准，极力推荐 🌟）

**核心思想**：将繁重的运算和内存开销剥离出浏览器，转交给服务器。

### 1. 架构流程
1. **前端动作**：用户在界面勾选需要导出的多个测井文件及过滤条件（如深度、AC、DEN等），点击“批量打包导出”。
2. **后端接收**：Spring Boot 后端统一接收这些文件 ID 和检索条件。
3. **流式生成**：Java 后端利用 `SXSSFWorkbook`（针对海量数据的流式 Excel 处理）或纯 CSV 格式，边从数据库/磁盘读取，边写入到压缩缓冲流（`ZipOutputStream`）中。
4. **返回给前端**：不需要在服务器生成庞大的临时文件，直接将 ZIP 数据流写入到 `HttpServletResponse` 返回。前端直接触发浏览器默认的下载行为，保存为 `测井数据批量导出_2026xxxx.zip`。

### 2. 优点与缺点
* **优点**：彻底解决浏览器内存溢出（OOM）和页面假死问题；支持 GB 级别的数据导出；用户体验极佳（前端无感知卡顿）。
* **缺点**：需要开发新的后端接口，增加少许服务端 CPU 和 I/O 负担。

---

## 方案二：前端串行队列 + JSZip 分块压缩（前端独立解决版）

**核心思想**：避免“同时”在内存中生成多个 Excel，改用“排队”模式，逐个生成、压缩后销毁内存。

### 1. 架构流程
1. **引入压缩模块**：使用第三方的 `jszip` 库。
2. **任务队列化**：把原来 `for` 循环全开的处理方式，改为通过 `async/await` 串行执行的异步队列。
3. **处理流程**：
   * 拿第 1个文件数据 -> 转化为 CSV 文本字符串 -> 送入 JSZip 实例 -> **清空释放该文件的数据内存 (GC)**。
   * 拿第 2个文件数据 -> 重复上面处理 -> 清空释放。
   * 所有的处理完毕后，JSZip 生成最后的 Blob 下载。
4. *(可选提升)* **Web Worker**：将上述逻辑放进 Web Worker 线程执行，不占用主线程，防止长达几秒几十秒的页面白屏和卡顿。

### 2. 优点与缺点
* **优点**：纯前端实现，不需要后端改动接口。采用队列和纯文本（CSV）压缩能省下非常可观的内存。
* **缺点**：由于最终的 ZIP Blob 仍然要在内存中组装，如果文件总量依然极大（如 500MB 以上），在低配电脑上依然有崩溃风险；生成速度慢于后端。

---

## 方案三：使用 File System Access API（前沿浏览器专属）

**核心思想**：越过内存限制，让前端代码像桌面软件一样直接往本地硬盘流式地（Stream）写文件。

### 1. 架构流程
1. **选择保存目录**：通过 `window.showDirectoryPicker()` 弹出文件夹选择框，让用户授权一个本地文件夹。
2. **读写流直接落盘**：
   * 循环各个选中的文件，向授权的本地文件夹中创建对应的 `.csv` 或 `.xlsx` 句柄。
   * 通过 `FileSystemWritableFileStream`，边计算前端处理的数据，边 `write()` 进入用户硬盘。
3. **完成机制**：处理完一个就彻底写盘结束。

### 2. 优点与缺点
* **优点**：纯前端方案中的终极解决解法，0 内存积压，速度极快，理论上限由硬盘存储决定。
* **缺点**：**兼容性受限**（目前几乎只有 Chrome/Edge 支持，Safari、Firefox较差）；且必须在 HTTPS 协议下或者 localhost 下才能唤起浏览器授权。

---

## 总结与开发建议

如果您打算彻底根治该问题并在企业级产品中使用：
**实施方案一（后端打流 ZIP）是最高效、最正统的路线。**

如果您现阶段**不想修改后端代码**，但仍需凑合出一个能用的功能，可以先尝试 **方案二的 Web Worker + 队列+直接输出纯文本(CSV)而非复杂 Excel 对象**。这样压缩率高且速度较快。