# 地质测井数据分析系统 — 技术细节文档

## 1. 文件上传

### 1.1 存储位置

上传目录通过 `application.yml` 配置，支持多个备用路径，按顺序尝试：

```yaml
app:
  upload:
    dirs:
      - E:\Others\upload
      - D:\upload
      - C:\project-upload
```

启动时 `@PostConstruct` 解析第一个可用目录，运行时动态检查（防止目录中途失效后自动降级到下一个）。

### 1.2 上传流程（两阶段沙盒）

```
用户上传文件
  │
  ├─ POST /file/preview
  │   ├─ 文件保存到上传目录（UUID + 原始文件名，防重名）
  │   ├─ 调用 previewTxtStreamSync() 做同步预览
  │   │   ├─ 读取前 100 行探测表头（取得分最高的行）
  │   │   ├─ 用字典别名正则匹配列名
  │   │   └─ 提取前 50 行预览数据
  │   └─ 返回：建议列映射 + 原始表头 + 预览数据 + 临时文件路径
  │
  └─ POST /file/confirm
      ├─ 前端确认/修改列映射后提交
      ├─ 创建 log_file_info 记录（status=0 解析中）
      ├─ 投递到异步线程池 logFileExecutor
      └─ 返回"任务已提交"，不阻塞前端
```

### 1.3 服务器目录扫描

```
GET /file/scan?path=xxx
  │
  ├─ 递归扫描目录下所有 .txt/.csv/.xls/.xlsx 文件
  ├─ 同名文件自动跳过
  ├─ 每个文件调用 previewTxtStreamSync() 获取建议映射
  ├─ 直接应用建议映射（不弹预览沙盒）
  └─ 逐个投递异步解析任务
```

### 1.4 文件大小限制

- 单文件上限：50MB（`spring.servlet.multipart.max-file-size`）
- 单次请求上限：500MB（`max-request-size`）

---

## 2. 文件解析

### 2.1 编码检测

所有 TXT/CSV 文件在读取前自动检测编码：

```
读取前 8KB 字节
  ├─ 跳过 UTF-8 BOM（EF BB BF）
  ├─ 逐字节校验是否为合法 UTF-8 多字节序列
  │   ├─ 合法 → 使用 UTF-8
  │   └─ 非法 → 回退到 GBK
  └─ 全 ASCII → 默认 GBK
```

预览和异步解析两个阶段共用同一套检测逻辑。

### 2.2 表头探测（previewTxtStreamSync）

```
读取前 100 行
  │
  ├─ 逐行扫描：
  │   ├─ 按空格拆分每行
  │   ├─ 统计数字词数量 numCount
  │   ├─ 统计字典匹配数 matchCount
  │   ├─ 数字过半 → 判定数据开始，停止探测
  │   └─ 计算得分 = matchCount / 总词数 × 100
  │
  ├─ 取得分最高的行作为表头
  │
  └─ 兜底：所有行都没有匹配 → 用第一行，列名设为 Col1, Col2, Col3...
```

### 2.3 列名映射

字典表 `sys_column_mapping` 存储别名规则：

| 标准列名 | 别名列表 |
|---------|---------|
| depth | dept, tvd, 深度, 测深, md |
| ac | dt, _ac, 声波, 声波时差 |
| gr | _gr, gamm, 伽马, 自然伽马 |

预览时将字典编译为正则（`^(DEPTH|depth|dept|tvd|深度|测深|md)$`，大小写不敏感），逐个匹配表头词。匹配到的用标准名，未匹配的保留原始名。重名自动加后缀（AC_1, AC_2）。

### 2.4 异步解析入库（asyncParseAndSaveTxtStream）

```
异步线程池执行（核心4/最大10/队列50，CallerRunsPolicy 拒绝策略）
  │
  ├─ 编译字典规则（只编译一次，全批次复用）
  │
  ├─ 按文件类型分流读取：
  │   ├─ Excel (.xls/.xlsx)：EasyExcel SAX 监听器，逐行回调
  │   ├─ CSV：BufferedReader + 自动编码检测，逗号转空格
  │   └─ TXT：BufferedReader + 自动编码检测
  │
  ├─ 每行处理（processParsedLine）：
  │   ├─ 按空格拆分
  │   ├─ 数据行判定：半数以上是数字
  │   ├─ 脏数据检查：列数偏差 > 3 或 < 一半 → 写入 log_dirty_data
  │   ├─ 按位置对应列名：第 N 个值 → 第 N 个列名
  │   ├─ 数字正则判断：是数字 → 格式化为 4 位小数
  │   └─ 攒入 batch
  │
  └─ 每 2000 行提交一次 processAndSaveBatch()
```

### 2.5 入库归列（processAndSaveBatch）

```
遍历 batch 中每行的每个 key-value：
  │
  ├─ 用字典正则匹配 key
  │   ├─ 匹配 depth → record.setDepth(val)
  │   ├─ 匹配 ac    → record.setAc(val)
  │   ├─ 匹配 gr    → record.setGr(val)
  │   ├─ 匹配 den/sp/rt → 对应 setter
  │   └─ 无匹配     → extraJson.put(key, val)
  │
  └─ MyBatis-Plus saveBatch 批量写入 log_data_records
```

### 2.6 数据库存储结构

`log_data_records` 大宽表：

| 列 | 类型 | 说明 |
|---|------|------|
| depth | DECIMAL(10,4) | 深度（核心列，有索引） |
| ac | DECIMAL(10,4) | 声波时差（核心列） |
| den | DECIMAL(10,4) | 岩石密度（核心列） |
| gr | DECIMAL(10,4) | 自然伽马（核心列） |
| sp | DECIMAL(10,4) | 自然电位（核心列） |
| rt | DECIMAL(10,4) | 深电阻率（核心列） |
| col_01~col_05 | DECIMAL(10,4) | 预留扩展列（当前未使用） |
| extra_json | JSON | 非核心列统一存入 JSON |

索引：`idx_file_depth(file_id, depth)` + `idx_file_main(file_id, ac, den, gr)`

---

## 3. 数据查询与筛选

### 3.1 分页查询（POST /data/page）

```
前端提交：{ fileId, current, size, filters: { GR: {min: 100, max: 200} } }
  │
  ├─ 构建 QueryWrapper：
  │   ├─ 核心列（depth/ac/den/gr/sp/rt）→ 直接 ge/le 条件
  │   └─ 非核心列 → JSON_EXTRACT(extra_json, CONCAT('$.', col)) >= val
  │
  ├─ MyBatis-Plus 分页查询（单页上限 10000 条）
  │
  └─ 按 log_file_info.columns_json 的列顺序组装返回数据
```

### 3.2 全局筛选（多文件统一）

```
前端全局筛选字段 = 所有已选文件 columns 的并集（Set 去重）
  │
  ├─ 用户设条件后点"统一提取全部"
  ├─ 将全局条件同步复制到每个 tab 的 filters
  └─ 逐个 tab 调用 fetchPageData
```

注意：全局筛选对所有文件使用同一组条件。如果某个文件没有该列，后端自动忽略（不会报错）。

---

## 4. 异常测段提取

### 4.1 连续段检测算法

```
前端逐行遍历当前文件的筛选结果：
  │
  ├─ 对每行检查是否满足条件（如 GR >= 100）
  │
  ├─ 满足 → 加入当前连续段
  │   ├─ 记录 startDepth, endDepth, pointCount, sumValue
  │   └─ 更新 endDepth = max(endDepth, 当前深度)
  │
  ├─ 不满足 → 关闭当前段
  │   └─ pointCount >= 最短连续测点数 → 保留该段
  │
  └─ 最终输出：每个异常段的顶界/底界/厚度/均值
```

### 4.2 多文件统一提取

对所有已选文件使用同一组条件，逐个文件执行连续段检测，合并结果。

---

## 5. 可视化（ECharts）

### 5.1 数据加载策略（GET /data/echarts/{fileId}）

```
查询总记录数
  │
  ├─ ≤ 5000 行 → 全量加载，直接返回
  │
  └─ > 5000 行 → 分页加载（每页 5000 条）→ LTTB 降采样至 5000 点
```

### 5.2 LTTB 降采样算法

```
Largest Triangle Three Buckets：
  │
  ├─ 将 N 个点分为 (targetPoints - 2) 个桶
  ├─ 第一个点和最后一个点始终保留
  ├─ 每个桶中选一个点：
  │   └─ 与上一个选中点 + 下一个桶平均值 形成三角形
  │   └─ 选三角形面积最大的点
  └─ 效果：保留尖峰/突变，压缩平缓区域
```

返回格式：`{ DEPTH: [...], GR: [...], AC: [...], ... }`，列式数组。

---

## 6. 数据导出

### 6.1 单文件 Excel 导出（POST /data/{fileId}/export）

```
前端提交筛选条件
  │
  ├─ 创建 ExcelWriter → response.getOutputStream()
  ├─ streamWriteData() 流式分页写入：
  │   ├─ 每页查询 EXPORT_BATCH_SIZE = 10000 条
  │   ├─ 查询条件与分页查询一致（支持筛选后导出）
  │   ├─ 每页数据立即写入 ExcelWriter
  │   └─ 累计上限 EXPORT_MAX_TOTAL = 1,000,000 行
  └─ 关闭 writer，浏览器下载 .xlsx 文件
```

### 6.2 批量 ZIP 导出（POST /data/export-batch-zip）

```
前端提交多个文件的查询条件
  │
  ├─ 创建 ZipOutputStream → response.getOutputStream()
  ├─ 逐个文件：
  │   ├─ 创建 ZipEntry（文件名.xlsx）
  │   ├─ streamWriteData() 流式写入该文件数据
  │   ├─ 关闭 entry
  │   └─ 累计统计导出文件数和行数
  └─ zos.finish()，浏览器下载 .zip 文件
```

### 6.3 异常段 CSV 导出

前端直接生成，不走后端。将异常段列表拼成 CSV 字符串，用 Blob 下载。

---

## 7. 清除缓存（POST /sys/log/cleanup）

用户手动触发，按顺序执行 5 条清理 SQL：

| 步骤 | SQL | 说明 |
|------|-----|------|
| 1 | `DELETE FROM log_data_records WHERE file_id IN (SELECT id FROM log_file_info WHERE status = 2)` | 清理已删除文件的明细数据 |
| 2 | `DELETE FROM log_dirty_data WHERE file_id IN (SELECT id FROM log_file_info WHERE status = 2)` | 清理已删除文件的脏数据 |
| 3 | `DELETE FROM log_file_info WHERE status = 2` | 物理删除已标记删除的文件元数据 |
| 4 | `DELETE FROM sys_operation_log WHERE create_time < DATE_SUB(NOW(), INTERVAL 30 DAY)` | 删除 30 天前的操作日志 |
| 5 | `DELETE FROM sys_refresh_token WHERE revoked = 1 OR expires_at < NOW()` | 删除已撤销或已过期的刷新令牌 |

返回每步的清理数量。单机版不做定时任务，由用户主动触发。

---

## 8. 文件删除

文件删除是**逻辑删除**：

```
DELETE /file/{id}
  ├─ 将 log_file_info.status 设为 2（逻辑删除）
  └─ 不立即删除物理数据和磁盘文件

实际物理清理在"清除缓存"时统一执行
```

---

## 9. 认证机制

### 9.1 JWT 双令牌

| | Access Token | Refresh Token |
|--|-------------|---------------|
| 有效期 | 1 小时 | 7 天 |
| 用途 | API 请求凭证 | 换取新的 Access Token |
| 存储 | 前端 localStorage + 内存 | 前端 + 数据库（只存 SHA-256 哈希） |
| 生成 | JWT 签名（含 username + userId） | 48 字节 SecureRandom + Base64 |

### 9.2 Token 轮换

```
Access Token 过期
  → 前端 Axios 拦截器自动捕获 401
  → 用 Refresh Token 请求 POST /auth/refresh
  → 后端验证 Refresh Token（SHA-256 哈希匹配 + 未撤销 + 未过期）
  → 生成新的 Access Token + 新的 Refresh Token
  → 旧 Refresh Token 标记 revoked=1，记录 replaced_by
  → 返回新令牌对
  → 前端替换旧令牌，继续请求（用户无感）
```

### 9.3 密码存储

MD5 + 固定盐值 `xwlog#2026`：`MD5(password + salt)`

---

## 10. 数据库表清单

共 8 张表：

| 表名 | 用途 |
|------|------|
| sys_user | 用户（含 role, email, introduction, sys_settings） |
| log_file_info | 文件元数据（文件名, 列配置 JSON, 行数, 状态） |
| log_data_records | 测井明细大宽表（6 核心列 + 5 预留列 + JSON 扩展） |
| log_dirty_data | 解析失败的脏数据 |
| sys_column_mapping | 列名映射字典（标准名, 别名, 是否核心列） |
| sys_operation_log | 操作日志（模块, 描述, 文件数, 行数） |
| sys_instruction | 使用说明（标题, 图标, HTML 内容） |
| sys_refresh_token | 刷新令牌（hash, 过期时间, 撤销状态, 替换记录） |

---

## 11. 性能设计要点

| 场景 | 方案 | 内存占用 |
|------|------|---------|
| 文件解析 | BufferedReader 流式 / SAX 监听器 + 异步线程池 | < 50MB |
| 数据浏览 | MyBatis-Plus 分页，单页上限 10000 | 极低 |
| 解析报告 | SQL 聚合统计（MIN/MAX/COUNT），不加载全量数据 | 极低 |
| ECharts | 分页加载 + LTTB 降采样至 5000 点 | < 20MB |
| Excel 导出 | 分页查询 + ExcelWriter 流式写入，上限 100 万行 | < 50MB |
| 字典规则 | 一次编译全批次复用 | 极低 |
| 仪表盘刷新 | onActivated 页面激活时自动刷新 | 极低 |
