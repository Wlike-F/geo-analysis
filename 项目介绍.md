# 地质测井数据分析系统

## 项目简介

面向地质勘探的测井数据分析工具，支持 TXT、CSV、Excel 格式的文件上传、解析、筛选、可视化和导出。前后端分离架构，单机部署，所有数据在本地处理和存储，不走云端。

---

## 技术栈

### 前端

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue 3 | 3.4 | Composition API |
| Vite | 5.1 | 构建工具 |
| Element Plus | 2.6 | UI 组件库 |
| Pinia | 2.1 | 状态管理 |
| ECharts | 6.0 | 测井曲线可视化 |
| Vue Router | 4.3 | 路由 + 权限守卫 |
| Axios | 1.6 | HTTP 请求，含 Token 自动刷新 |

### 后端

| 技术 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 3.2.4 | Web 框架 |
| Java | 17 | |
| MyBatis-Plus | 3.5.5 | ORM + 分页 |
| MySQL | 8.0+ | 主数据库 |
| Redis | 6.x+ | 会话缓存 |
| EasyExcel | 3.3.3 | Excel/CSV 解析与导出 |
| JJWT | 0.11.5 | JWT 双令牌认证 |
| Hutool | 5.8.25 | 工具库 |

---

## 功能模块

### 1. 仪表盘首页

- 工作台模式：展示最近上传的文件及其解析状态（解析中/失败/成功），待处理文件优先显示
- 4 个统计卡片：已加载文件数、累计解析行数、导出次数、系统状态（数据来自真实统计，非日志累加）
- 快捷操作网格：数据源管理、测井数据看板、异常测段提取、曲线图表
- 最新活跃记录时间线

### 2. 数据源管理

- 文件上传：支持 `.txt`、`.csv`、`.xls`、`.xlsx`
- 智能预览：上传后自动探测表头、识别列名、生成建议映射，确认后异步入库
- 目录扫描：指定服务器本地路径，递归扫描并批量提交解析
- 编码处理：CSV 自动检测 UTF-8/GBK，TXT 默认 GBK
- 流式处理：TXT/CSV 用 BufferedReader，Excel 用 SAX 监听器，50 万行内存占用 < 50MB

### 3. 测井数据看板

- 分页浏览，按列筛选（最小值/最大值范围）
- 动态列展示，根据文件实际列配置自适应
- 单文件 Excel 导出、多文件批量 ZIP 导出

### 4. 异常测段提取

- 多文件统一筛选 / 单文件独立筛选
- 连续段检测：自动识别满足条件的连续测段，输出顶界深度、底界深度、厚度等
- 支持 Excel 和 ZIP 格式导出

### 5. 异常测段可视化

- 最多 3 条测井曲线同屏展示
- 异常段红色高亮标注
- 深度区间滑块缩放
- 后端 LTTB 降采样：50 万行 → 5000 点，秒级渲染
- 支持导出 PNG 图片和 CSV 明细

### 6. 字典映射参数

- 列名别名归一化（如 DEPT/TVD/深度 → DEPTH）
- 核心列受保护不可删除
- 解析时自动匹配应用

### 7. 使用说明

- 10 个主题，覆盖快速开始、核心功能操作指引、常见问题排查
- 每个主题包含操作步骤、注意事项和页面跳转链接
- 侧边栏目录导航 + 全文搜索

### 8. 系统设置

- **个人信息**：姓名、邮箱、简介、头像
- **安全隐私**：修改密码、登录日志、存储用量查看、清除缓存
- **操作日志**：全量操作历史，按模块分类（文件/导出/登录等），分页浏览

---

## 数据本地化

系统设计原则是**数据不出本地**：

- 文件存在服务器本地磁盘，不上传云端
- 解析、分析、导出全在本地完成，没有外部 API 调用
- MySQL 部署在本地，用户数据、测井数据、操作日志全部本地存储
- 可以部署在内网环境，不需要公网

---

## 认证与权限

- JWT 双令牌：Access Token（1h）+ Refresh Token（7d），自动轮换
- 前端 Axios 拦截器自动刷新，用户无感
- 角色区分：管理员 / 普通用户，菜单按角色动态显示
- 数据隔离：每个用户只能看自己的文件

---

## 性能优化（50 万行级别）

| 场景 | 方案 | 效果 |
|------|------|------|
| 文件解析 | BufferedReader 流式 + SAX 监听器 + 异步线程池 | 内存 < 50MB |
| 分页查询 | MyBatis-Plus 分页，单页上限 10000 | 毫秒级 |
| 解析报告 | SQL 聚合（MIN/MAX/COUNT），不加载全量数据 | < 1 秒 |
| ECharts | 分页抽样 + LTTB 降采样至 5000 点 | JSON < 500KB |
| Excel 导出 | 分页查询 + ExcelWriter 流式写入，上限 100 万行 | 内存稳定 |
| 字典规则 | 一次编译全批次复用 | 省 250 次 DB 查询 |

---

## 数据清理机制

单机版不做定时任务，改为手动触发：

- 系统设置页提供「清除缓存」按钮，确认后一键清理：
  1. 已删除文件（status=2）的明细数据
  2. 已删除文件的脏数据
  3. 已删除文件的元数据
  4. 30 天前的操作日志
  5. 已撤销或已过期的刷新令牌
- 清理完成后展示具体清理数量

---

## 项目结构

```
xy_txt_ui/
├── front-end/                  # 前端（Vue 3 + Vite）
│   ├── src/
│   │   ├── api/                # API 接口封装
│   │   ├── assets/             # 静态资源
│   │   ├── layout/             # 布局组件
│   │   ├── router/             # 路由配置
│   │   ├── store/              # Pinia 状态管理
│   │   ├── utils/              # 工具函数（请求拦截、Token 管理）
│   │   └── views/              # 页面组件
│   │       ├── dashboard/      # 仪表盘首页（工作台模式）
│   │       ├── datasource/     # 数据源管理
│   │       ├── files/          # 测井数据看板
│   │       ├── extract/        # 异常测段提取
│   │       ├── visualization/  # 曲线图表
│   │       ├── instructions/   # 使用说明
│   │       ├── settings/       # 系统设置 & 字典映射
│   │       ├── user/           # 用户管理
│   │       ├── login/          # 登录
│   │       └── register/       # 注册
│   └── package.json
├── back-end/                   # 后端（Spring Boot 3）
│   ├── src/main/java/com/xy/welllog/
│   │   ├── controller/         # 控制器层
│   │   ├── service/            # 业务逻辑层
│   │   ├── mapper/             # MyBatis-Plus Mapper
│   │   ├── entity/             # 数据实体
│   │   ├── dto/                # 数据传输对象
│   │   ├── config/             # 配置类（CORS、线程池、Redis、MyBatis）
│   │   ├── utils/              # 工具类（JWT、文件处理）
│   │   └── exception/          # 全局异常处理
│   └── pom.xml
├── database/
│   └── init.sql                # 数据库初始化脚本（8 张表）
└── data/                       # 示例测井数据 + 50万行测试数据生成脚本
```

---

## 数据库设计

共 8 张表：

| 表名 | 说明 |
|------|------|
| sys_user | 用户表（含 role、email、introduction、sys_settings） |
| log_file_info | 测井文件元数据（文件名、列配置、行数、状态） |
| log_data_records | 测井明细宽表（固定列 depth/ac/den/gr/sp/rt + 5 个预留列 + extra_json） |
| log_dirty_data | 解析失败的脏数据记录 |
| sys_column_mapping | 列名映射字典（标准名、别名、是否核心列） |
| sys_operation_log | 操作日志（模块、描述、文件数、行数） |
| sys_instruction | 使用说明（标题、图标、HTML 内容） |
| sys_refresh_token | 刷新令牌（hash、过期时间、撤销状态） |
