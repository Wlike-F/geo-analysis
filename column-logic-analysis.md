# 测井数据列名解析与多文件筛选架构重构方案

经过源码与数据解析逻辑的深度剖析，您刚刚反馈的“**一个文件明明7列，多文件加载后筛选框冒出了10多列**”的现象，切中了当前系统最核心的**数据异构兼容性（雷点）**。这不仅仅是一个UI显示的Bug，更是底层逻辑缺乏统一“别名映射机制”导致的业务隐患。

下面为您全面梳理当前的风险（雷点）并给出重构的架构思路。

---

## 一、 当前的致命“雷点”解析

### ⚡ 雷点 1：列名缺乏“归一化（Alias）”，导致界面爆炸
您的文件 A（如 `le7.txt`）的列头是：`DEPTH`, `AC`, `DEN`, `GR`。
您的文件 B（如 `LT2.txt`）的列头是：`井号`, `深度`, `TVD`, `GR`, `_RT`, `_SP`, `_AC`。

此时，前端全局筛选去做了列名合并（Union 取并集），并没有将物理意义相同的列合并，最终筛选项变成了高达 10 个独立字段：`DEPTH`, `AC`, `DEN`, `GR`, `井号`, `深度`, `TVD`, `_RT`, `_SP`, `_AC`。如果不处理，以后挂载100个测井文件，筛选框可能膨胀出 50 多个莫名其妙的字段（如 `DEPT`, `MD`, `DT` 等）。

### ⚡ 雷点 2：跨文件联合筛选的“互相封杀”漏洞（最致命 Bug）
这是由全量取并集导致的最严峻的业务 Bug：
* **问题场景**：用户想筛选 100m~200m 的测段。于是用户在界面的 `DEPTH` 框输入了 100 - 200，并点击统一筛选。
* **后端的灾难**：文件 A 顺利筛出了数据；但文件 B 收到这个指令后，去寻找名为 `DEPTH` 的列，发现自己没有（它叫 `深度` 或 `TVD`），导致判断结果为 `Null` 返回 **0条记录**。
* **更绝望的场景**：如果用户为了“照顾”两个文件，把 `DEPTH` 和 `深度` 框**都填上**了 100 - 200。此时后台对文件A进行 `(DEPTH匹配) && (深度匹配)`（深度报Null -> 0条数据），对文件B进行 `(深度匹配) && (DEPTH匹配)`（DEPTH报Null -> 0条数据）。**最终结果是所有文件都被置空，多文件筛选彻底瘫痪。**

### ⚡ 雷点 3：数据库标准宽表未起作用
后端代码（`processAndSaveBatch`）目前只有非常简陋的匹配逻辑：
* 只认全小写精确匹配：`depth`/`ac`/`den`/`gr` 才能写入关系型字段并创建索引。
* 其余（如 `_AC`、`_SP`、`深度`）通通被视作未知扩展属性，扔进了 `extra_json` 文本长字段。这会导致未来大数据量检索时，因为 `_AC` 没走到原生索引，性能大大拖慢。

---

## 二、 解决业务思路：引入“标准化别名字典”

要彻底扫雷，我们需要在**数据上传接入期**到**界面渲染期**之间，加上一层坚固的“**标准化列名洗注**”引擎。

### 第 1 步：建立映射词典配置（Backend）
我们在后端建立一套物理意义的标准名称库，一旦发现类似词根，全部归化（映射）为标准键名：
* **标准 `DEPTH`**：匹配 `depth`, `dept`, `tvd`, `深度`, `测深`, `md`
* **标准 `AC` (声波)**：匹配 `ac`, `dt`, `_ac`, `声波`
* **标准 `DEN` (密度)**：匹配 `den`, `zden`, `rhob`, `密度`
* **标准 `GR` (伽马)**：匹配 `gr`, `_gr`, `伽马`, `gamm`
* **标准 `SP` (自然电位)**：匹配 `sp`, `_sp`, `电位`
* **标准 `RT` (电阻率)**：匹配 `rt`, `_rt`, `ild`
* **未知扩展 `extra`**：无法匹配字典的，原样保留（如 `井号` 等）。

### 第 2 步：洗数据留存证（Database/Service）
在 TXT 解析进数据库前，自动替换 Headers（例如将 `_AC` 剔除，替换成标准 `AC`）。
入库时，所有匹配上的核心指标全部写进 MySQL 标量的独立浮点型原生字段，极大加快索引速度；剩余的写进 `extra_json`。

### 第 3 步：全局筛选界面的净化（Frontend）
经过这样的洗注，文件 A 和 文件 B 上传后告诉前端的列都会变成：`[DEPTH, AC, DEN, GR]` 和 `[井号, DEPTH, GR, RT, SP, AC]`。
此时，前端再做并集收集，得到的筛选项就会非常干净：`[DEPTH, AC, DEN, GR, 井号, RT, SP]`！没有冗余的废弃变量。
用户全局筛选时，只需要在唯一的 `DEPTH` 里输入条件，文件A 和 A文件B 的底层 `DEPTH` 本质被一同正确命中！完美解决多文件异构查询拉黑现象。

---

## 三、 接下来的执行计划建议

为了实现这套强大的标准化方案，需要您确认是否允许我进行以下深度的前后端改造：
1. 重写 Java 后端 `FileController.java` 和 `LogDataRecordServiceImpl.java` 中生硬的解析机制，引入基于正则的字典映射（RegEx Alias Dictionary）与过滤转化拦截器。
2. 重洗并保留旧有的映射（如 `LogFileInfo` 的 `ColumnsJson` 将储存洗注后的规范名字列）。
3. 前端只认这套被洗过后的标准词库做 `Tabs`。
（如果现阶段库中存在之前的包含乱别名列的脏文件记录，可能会要求您点击一次页面的 `清空并重置看板`，重新加载 TXT 以套用新引擎）